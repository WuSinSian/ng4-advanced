import { BtnNumberPlusDirective } from './btn-number-plus.directive';

describe('BtnNumberPlusDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnNumberPlusDirective();
    expect(directive).toBeTruthy();
  });
});
